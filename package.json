{
  "name": "barkada-agent-proxy",
  "version": "1.0.0",
  "description": "Simple proxy to forward agent queries to OpenAI for the Barkada site",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "axios": "^1.4.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.2"
  }
}
